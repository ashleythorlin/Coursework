// lab 14.cpp : This file contains the 'main' function. Program execution begins and ends there.

/*
#include <iostream>
using namespace std;

int reverseString(string, int, int);

int main()
{
    string inputString = "stressed";
    cout << "The unaltered string is:" << endl;
    cout << inputString << endl;
    cout << "The string backwards is:" << endl;
    reverseString("stressed", 0, 0);
    return 0;
}

int reverseString(string inputString, int position, int runCount)
{
    if (position == 0 && runCount == 0)
        position = inputString.length() - 1;
    if (position > -1)
    {
        cout << inputString[position];
        runCount++;
        return reverseString(inputString, position - 1, runCount);
    }
    else
        return 0;
}
*/
